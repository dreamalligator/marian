#!/usr/bin/env bash

echo 'running serve script...'

# sudo ufw allow 5000
# $ sudo ufw allow ssh
# $ sudo ufw allow http
# $ sudo ufw allow 443/tcp
# $ sudo ufw --force enable
# $ sudo ufw status
# python marian
# gunicorn "marian.app:create_app()"
# gunicorn -b localhost:8000 -w 4 marian

echo 'done.'
